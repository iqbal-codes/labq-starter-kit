---
import { socialProofStats, testimonials } from "../../data";
import { Container, Card, StarRating } from "../ui";
---

<section class="bg-muted/40 py-20">
  <Container>
    <div class="text-center">
      <h2 class="text-3xl font-bold tracking-tight text-foreground sm:text-4xl text-wrap: balance">
        Trusted by teams that ship
      </h2>
      <p class="mt-2 text-lg text-muted-foreground text-wrap: pretty">
        Strategy, execution, and measurable outcomes — all in one partnership.
      </p>
    </div>

    <div class="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
      {socialProofStats.map((stat) => (
        <Card className="p-5 text-center bg-background/70">
          <p class="text-2xl font-semibold tracking-tight text-foreground">{stat.value}</p>
          <p class="mt-2 text-sm text-muted-foreground">{stat.label}</p>
        </Card>
      ))}
    </div>

    <div class="mt-12 grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
      {testimonials.map((testimonial) => (
        <Card className="p-8 h-full flex flex-col justify-between">
          <div>
            <StarRating rating={5} size={16} className="mb-4" />
            <blockquote class="text-sm leading-relaxed text-muted-foreground italic">
              &ldquo;{testimonial.quote}&rdquo;
            </blockquote>
          </div>
          <figcaption class="mt-6 flex items-center gap-3">
            <div class="flex h-9 w-9 items-center justify-center rounded-full bg-muted text-xs font-semibold text-muted-foreground select-none">
              {testimonial.name.split(" ").map((part) => part[0]).join("")}
            </div>
            <div>
              <p class="text-sm font-medium text-foreground">{testimonial.name}</p>
              <p class="text-xs text-muted-foreground">{testimonial.role}</p>
            </div>
          </figcaption>
        </Card>
      ))}
    </div>
  </Container>
</section>
